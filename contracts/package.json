{
  "name": "@right-to-be-forgotten/contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:local": "hardhat run deploy/deploy-consent-registry.ts --network localhost",
    "deploy:amoy": "hardhat run deploy/deploy-consent-registry.ts --network polygonAmoy",
    "clean": "hardhat clean",
    "coverage": "hardhat coverage"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0",
    "typescript": "^5.4.0",
    "ts-node": "^10.9.0",
    "@types/node": "^22.0.0",
    "dotenv": "^16.4.0"
  }
}
